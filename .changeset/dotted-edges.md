---
'@archdraw/core': patch
---

Draw a `style: dashed` edge as round dots (`1 5`) rather than a `5 4` dash. Container boundaries are dashed `6 4` in the same grey, and an edge leaving a container routes around it — so the two read as one thing, and a telemetry edge looked like a second border.
