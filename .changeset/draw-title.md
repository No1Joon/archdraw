---
'@archdraw/core': minor
---

Draw a diagram's `title` above the graph. It only ever reached the SVG's accessible name, so an exported PNG carried no heading at all. The band is added to the canvas rather than to ELK's box, and a title wider than the graph widens the canvas.
