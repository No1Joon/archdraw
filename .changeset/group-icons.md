---
'@archdraw/core': minor
---

A container may now carry `type` to show its service icon beside the group label, so a VPC or an instance can be labelled with the vendor mark the way reference architectures draw it.
