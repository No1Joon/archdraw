---
'@archdraw/core': minor
---

Add `shape: card` — the mark on the left with the name beside it, the way Google Cloud draws a service. `icon` (name under the mark, the AWS convention) stays the default, and a node may override the diagram's choice.
